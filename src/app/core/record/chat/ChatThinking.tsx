import type { Chat } from "@/db/chats";
import React from "react";

interface ChatThinkingProps {
	chat: Chat;
}

const ChatThinking = ({}: ChatThinkingProps) => {
	return <div>ChatThinking</div>;
};

export default ChatThinking;
