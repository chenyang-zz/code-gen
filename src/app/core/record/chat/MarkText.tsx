import type { Chat } from "@/db/chats";
import React from "react";

interface MarkTextProps {
	chat: Chat;
}

const MarkText = ({}: MarkTextProps) => {
	return <div>MarkText</div>;
};

export default MarkText;
